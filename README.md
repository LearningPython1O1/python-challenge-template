# Défi Python 🐍

Bienvenue dans ton défi de code Python !

## Objectif

Écris du code dans `main.py` pour faire passer tous les tests.

## Instructions

1. Code ta solution dans `main.py`.
2. Quand tu es prêt, fais un commit puis un push avec un **tag** (ex: `v1.0.0`).
3. Cela déclenchera les tests automatiquement via GitHub Actions.

Bonne chance !

